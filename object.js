let person = {
    name:"Aswin",
    age:20,
    place:"chottanikkara",
    address:{
        hname:"mothelil",
        postoff:"kanayannoor"
    }
};
//console.log(person.address)
let names =`${person.name} ${person.hname}`;
console.log(names)